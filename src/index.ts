//our first typescript code
let age : number =20; 
//we declared the variable age using the let keyword and we annotate(explain) it to a number and assigned a value of 20.

if (age < 30) {
    age += 10;
}
console.log(age);

